// ============================================================
// APP VERSION — placeholder file.
// The GitHub Actions workflow OVERWRITES this file automatically
// on every build with the real build number (see the
// "Generate version file" step in build_flutter_apk.yml), so you
// never need to edit this by hand. This local value is only used
// if you build the app yourself outside of that workflow.
// ============================================================
const String kAppVersion = 'dev';
